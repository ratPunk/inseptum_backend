RewriteEngine On

# Route all requests to index.php
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]

# Deny access to .env files
<Files ".env">
    Order allow,deny
    Deny from all
</Files>

# Deny access to PHP files in subdirectories (optional hardening)
# <FilesMatch "\.php$">
#     Order allow,deny
#     Deny from all
# </FilesMatch>
